name: Daily Update & Newsletter

# Ejecuta automáticamente todos los días a las 9:00 AM UTC
on:
  schedule:
    - cron: '0 9 * * *'  # 9 AM UTC cada día
  workflow_dispatch:  # También permite ejecutar manualmente

permissions:
  contents: write  # Permite hacer commits y push

jobs:
  update-and-send:
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout del código
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # 2. Configura Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      # 3. Instala dependencias
      - name: Install dependencies
        run: |
          npm install openai
          npm install @getbrevo/brevo
      
      # 4. Actualiza base de datos con IA
      - name: Update database with AI
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: node scripts/update-database.js
      
      # 5. Commit cambios si hay actualizaciones
      - name: Commit database updates
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add script.js
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Actualización diaria: nuevos descubrimientos"
          git push
      
      # 6. Envía newsletter diario
      - name: Send daily newsletter
        env:
          BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
        run: node scripts/send-daily-email.js
      
      # 7. Guarda historial de emails
      - name: Save email history
        run: |
          git add scripts/email-history.json || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "📧 Historial de envío actualizado" && git push) || true
